// Execute a currency converter. A user puts in USD, chooses a currency (EUR, SEK, AUD, etc.) and gets the result. 

let usdAmount = prompt("Add your USD");
let currency = prompt("Choose your currency (EUR, SEK, AUD)");

if (currency) {
    
}
